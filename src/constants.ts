/**
 * Constant values used in the application.
 *
 * @src/constants
 */

/**
 * The name of the cookie storing the JWT authenticating the user.
 */
export const COOKIE_AUTH_USER = 'min-nextjs_auth';

/**
 * The name of the flash cookie.
 */
export const COOKIE_FLASH = 'min-nextjs_flash';

/**
 * The generic separator value.
 */
export const SEP = '||';
