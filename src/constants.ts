/**
 * Constant values used in the application.
 *
 * @src/constants
 */

/**
 * The name of the cookie storing the JWT authenticating the user.
 */
export const COOKIE_AUTH_USER = 'min-nextjs_auth';

/**
 * The name of the flash cookie.
 */
export const COOKIE_FLASH = 'min-nextjs_flash';

/**
 * The default number of milliseconds before fetch requests throw errors due to timeout.
 */
export const DEFAULT_FETCH_TIMEOUT = 8000;

/**
 * Error code for unexpected errors relating to authentication.
 */
export const ERROR_1001 = 'E1001';

/**
 * Error code for unauthenticated requests.
 */
export const ERROR_1002 = 'E1002';

/**
 * Error code for expired sessions.
 */
export const ERROR_1003 = 'E1003';

/**
 * The HTTP DELETE verb.
 */
export const HTTP_DELETE = 'DELETE';

/**
 * The HTTP GET verb.
 */
export const HTTP_GET = 'GET';

/**
 * The HTTP POST verb.
 */
export const HTTP_POST = 'POST';

/**
 * The HTTP PUT verb.
 */
export const HTTP_PUT = 'PUT';

/**
 * The generic separator value.
 */
export const SEP = '||';
